# 一加15解锁激发亮度 v1.5

模块名称：`OnePlus 15 Slider HBM Unlock`

作者：傻瓜我爱你呀喵

## 功能

- 使用系统原生亮度滑块调节，刷入后不会强制屏幕一直处于最高亮度。
- 解锁原厂完整亮度曲线，系统逻辑上限从 `4095` 扩展到 `4674`。
- 最大逻辑值通过原厂 Apollo 曲线映射到面板背光码 `DBV 4094`，原厂曲线对应 `1800 nit` 档位。
- 处理 ColorOS 的前台应用降亮策略和窗口亮度限制，让应用尽量跟随系统亮度。

## 兼容

适用于一加 15 各系统版本。安装时只检查 OnePlus 15 机型属性（`PLK110`、`OP60FFL1` 或包含 `OnePlus 15`），不校验显示配置哈希；不是一加 15 的设备不会安装。

## 使用方法

1. 在 KernelSU 或兼容的 Root 模块管理器中刷入 ZIP。
2. 重启手机。
3. 直接调节系统亮度滑块即可生效。

## 注意事项

`4094` 是面板 DBV 背光码，不是实际的 `4094 nit`。实际显示亮度会受面板、温度、功耗、HDR、内容和 ColorOS 热保护影响，部分应用或场景仍可能存在差异。模块不执行强制拉满，也不会关闭系统热保护。

卸载模块后重启，可恢复原厂亮度范围。

下载：<https://github.com/AaTempSpoof/oneplus15-brightness-unlock/releases/latest>

SHA-256：`db1491299fdbd65f7a529c198fad5ae15a1c05be2cf1af4493afe139eacf9aea`
